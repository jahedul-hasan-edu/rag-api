"""
Document upload endpoints.

Not implemented in this foundation phase.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/documents", tags=["Documents"])

# Upload / ingest routes will be added in a later phase.
